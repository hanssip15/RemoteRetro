{
  "name": "remoteretro-refactor",
  "version": "1.0.0",
  "description": "RemoteRetro project refactored into separate frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run start:dev",
    "install:all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "start": "cd backend && npm run start:prod"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["retrospective", "remote", "team", "react", "nestjs"],
  "author": "RemoteRetro Team",
  "license": "MIT"
} 